/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'www.bing.com', 'images.unsplash.com', 'unsplash.com']
    },
};

module.exports = nextConfig
